<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.csi5230.salamdawood.mycare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MyPatientsActivity" />
        <activity android:name=".PatientInfoActivity" />
        <activity android:name=".UserHomeActivity" />
        <activity android:name=".DoctorHomeActivity" />
        <activity android:name=".AddEditPatientActivity" />
        <activity android:name=".RefillRequestActivity" />
        <activity android:name=".AddPrescriptionActivity" />
        <activity android:name=".ViewPrescriptionsActivity"></activity>
    </application>

</manifest>